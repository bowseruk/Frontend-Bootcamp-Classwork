// TODO: What will the following line of code log?
// This returns the window object, which desribes the browser window and has a lot of sub-objects
console.log(window);

// TODO: What will the following line of code log?
// This returns the HTML DOM for the page
console.log(window.document);

// TODO: What will the following line of code log?
// This returns the html elements of the page
console.log(document.documentElement);

// TODO: What will the following line of code log?
// This returns the HTML elements in the head of the page
console.log(document.head);

